(()=>{const wait=()=>{if(typeof db==='undefined'||typeof openDrawer!=='function'||typeof editMaster!=='function'||typeof save!=='function'||typeof masters!=='function')return setTimeout(wait,50);if(window.__jaMasterV5)return;window.__jaMasterV5=1;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));openClientHierarchy=function(){openDrawer('🏢 取引先・工場');drawerBody.innerHTML=db.clients.map(c=>`<div class="hier-card"><div class="hier-title"><div><b>${c.name}</b><div class="muted">${db.factories.filter(f=>f.clientId===c.id).length}工場</div></div><button class="btn mini" onclick="editMaster('client','${c.id}')">取引先を編集</button></div><div class="hier-sub">${db.factories.filter(f=>f.clientId===c.id).map(f=>`<div style="padding:9px 0;border-bottom:1px solid var(--line)"><div class="factory-title-row"><b>${f.name}</b>${f.code?`<span class="chip">${f.code}</span>`:''}<button class="btn icon-edit" onclick="editMaster('factory','${f.id}')">✎</button></div><div><small class="muted">部署</small><div>${db.sectors.filter(s=>s.factoryId===f.id).map(s=>`<span class="chip">${s.name}</span>`).join('')||'<span class="muted">なし</span>'}</div></div><div style="margin-top:6px"><small class="muted">班</small><div>${db.shifts.filter(s=>s.factoryId===f.id).map(s=>`<span class="chip">${s.name}</span>`).join('')||'<span class="muted">なし</span>'}</div></div><div class="hier-actions"><button class="btn mini" onclick="editMaster('sector','','${f.id}')">+ 部署を追加</button><button class="btn mini" onclick="editMaster('shift','','${f.id}')">+ 班を追加</button></div></div>`).join('')}</div></div>`).join('');drawerFoot.innerHTML=`<button class="btn" onclick="closeDrawer()">閉じる</button><button class="btn primary" onclick="editMaster('client','')">+ 取引先を追加</button>`};function fieldRow(f={}){const id=f.id||('field_'+Math.random().toString(36).slice(2,8));return `<div class="hier-card" data-doc-master-field data-id="${esc(id)}"><div class="grid2"><div class="field"><label>項目名 *</label><input data-k="label" value="${esc(f.label||'')}"></div><div class="field"><label>入力形式</label><select data-k="type"><option value="text" ${f.type==='text'?'selected':''}>テキスト</option><option value="date" ${f.type==='date'?'selected':''}>日付</option><option value="select" ${f.type==='select'?'selected':''}>選択肢</option></select></div><div class="field"><label>必須</label><select data-k="required"><option value="1" ${f.required?'selected':''}>必須</option><option value="0" ${!f.required?'selected':''}>任意</option></select></div><div class="field"><label>選択肢（選択肢の場合、1行1項目）</label><textarea data-k="options" rows="3">${esc((f.options||[]).join('\n'))}</textarea></div></div><div style="text-align:right"><button class="btn danger mini" type="button" onclick="this.closest('[data-doc-master-field]').remove()">項目を削除</button></div></div>`}function editDocType(id=''){const item=(db.docTypes||[]).find(x=>x.id===id)||{id:'',ja:'',pt:'',fields:[]};openDrawer(id?'書類種類を編集':'書類種類を追加');drawerBody.innerHTML=`<div style="max-width:900px"><div class="grid2"><div class="field"><label>書類名 *</label><input id="dmName" value="${esc(item.ja||item.name||'')}"></div><div class="field"><label>ポルトガル語名</label><input id="dmPt" value="${esc(item.pt||'')}"></div></div><div class="hml-section-head"><div><h3 style="margin:0">入力項目</h3><div class="muted">この書類を社員へ登録するときに表示する項目を設定します。</div></div><button class="btn" type="button" id="dmAddField">+ 項目を追加</button></div><div id="dmFields">${(item.fields||[]).map(fieldRow).join('')}</div><div class="notice">すべての書類で PDF または画像ファイルの添付が必須です。</div></div>`;drawerFoot.innerHTML=`<button class="btn" onclick="closeDrawer()">閉じる</button><button class="btn primary" id="dmSave">保存</button>`;document.getElementById('dmAddField').onclick=()=>document.getElementById('dmFields').insertAdjacentHTML('beforeend',fieldRow());document.getElementById('dmSave').onclick=()=>saveDocType(id)}function saveDocType(id){const name=document.getElementById('dmName').value.trim();if(!name){window.systemNotice?.('書類名を入力してください。','書類マスター');return}const fields=[...document.querySelectorAll('[data-doc-master-field]')].map(r=>{const type=r.querySelector('[data-k="type"]').value,label=r.querySelector('[data-k="label"]').value.trim();return{id:r.dataset.id,label,type,required:r.querySelector('[data-k="required"]').value==='1',options:type==='select'?r.querySelector('[data-k="options"]').value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean):[]}}).filter(x=>x.label);db.docTypes=db.docTypes||[];let item=db.docTypes.find(x=>x.id===id);if(item)Object.assign(item,{ja:name,pt:document.getElementById('dmPt').value.trim(),fields});else db.docTypes.push({id:'d_'+Date.now(),ja:name,pt:document.getElementById('dmPt').value.trim(),fields});save();closeDrawer();render();window.showSavedStatus?.()}const baseEdit=editMaster;editMaster=function(type,id,parent=''){if(type==='docType'){editDocType(id);return}baseEdit(type,id,parent);setTimeout(()=>{const titles={client:id?'取引先を編集':'取引先を追加',factory:id?'工場を編集':'工場を追加',sector:id?'部署を編集':'部署を追加',shift:id?'班を編集':'班を追加',bank:id?'銀行を編集':'銀行を追加',branch:id?'支店を編集':'支店を追加'};if(titles[type])drawerTitle.textContent=titles[type];const labels=[...drawerBody.querySelectorAll('label')];for(const l of labels){const t=l.textContent.trim(),map={'Nome':'名称','Código curto':'略称コード','Código':'コード','Cliente':'取引先','Fábrica':'工場','Setor':'部署','Banco':'銀行','Agência':'支店','Nome JP':'日本語名','Nome PT':'ポルトガル語名'};if(map[t])l.textContent=map[t]}},0)};function docMasterSection(){const types=db.docTypes||[];return `<section class="master-section"><div class="titlebar" style="margin-bottom:12px"><div><h2 style="margin:0">📄 書類マスター</h2><p>社員書類の種類と、登録時に必要な入力項目を設定します。</p></div><button class="btn primary" onclick="editMaster('docType','')">+ 書類種類を追加</button></div><div class="master-items">${types.map(t=>`<div class="master-item"><div><strong>${esc(t.ja||t.name||'書類')}</strong><small>${(t.fields||[]).map(f=>esc(f.label)).join('・')||'追加項目なし'} ／ PDF・画像必須</small></div><button class="btn mini" onclick="editMaster('docType','${t.id}')">編集</button></div>`).join('')||'<div class="muted">書類種類はまだ登録されていません。</div>'}</div></section>`}const baseMasters=masters;masters=function(){baseMasters();const host=document.getElementById('view');if(host&&!host.querySelector('[data-doc-master-section]')){const wrap=document.createElement('div');wrap.setAttribute('data-doc-master-section','1');wrap.innerHTML=docMasterSection();host.appendChild(wrap)}};})();