(()=>{
 const wait=()=>{if(typeof openEmployee!=='function'||typeof db==='undefined'||typeof by!=='function'||typeof save!=='function')return setTimeout(wait,60);if(window.__hmlHousingFinalV2)return;window.__hmlHousingFinalV2=1;
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const money=n=>n==null||n===''?'—':'¥'+Number(n||0).toLocaleString('ja-JP');
 const residents=d=>db.employees.filter(x=>x.dormId===d.id).length;
 const employeeCost=d=>{if(!d)return '';if(Number(d.capacity||1)>1&&Number(d.perPerson||0)>0)return Number(d.perPerson);return Number(d.rent||0)+Number(d.fee||0)};
 const equipment=d=>{const a=d?.appliances||[];return a.length?a.map(x=>x.type+(x.control?`（${x.control}）`:'')).join('・'):'なし'};
 const parking=d=>{if(!d)return '';const raw=d.parking??d.hasParking??d.parkingAvailable;if(raw===true||raw==='yes'||raw==='あり'||raw===1)return 'あり';if(raw===false||raw==='no'||raw==='なし'||raw===0)return 'なし';return '未登録'};
 const dormOpts=(sel,home)=>'<option value="">—</option>'+db.dorms.map(d=>`<option value="${d.id}" ${d.id===sel?'selected':''}>${esc(d.name)}</option>`).join('')+`<option value="__home__" ${home?'selected':''}>自宅</option>`;
 const isHome=e=>e.housingType==='home'||(!e.dormId&&!!e.address);
 const displayDate=s=>s?String(s).slice(0,10).replaceAll('-','/'):'—';
 function ensureHistory(e){e.housingHistory=e.housingHistory||[];if(!e.housingHistory.length&&(e.dormId||e.address)){const d=e.dormId?by(db.dorms,e.dormId):null;e.housingHistory.push({id:'housing_legacy',type:e.dormId?'dorm':'home',dormId:e.dormId||'',name:d?.name||'',address:e.address||d?.address||'',amount:d?employeeCost(d):'',start:e.housingStartDate||'',end:'',current:true})}}
 function historyHtml(e){ensureHistory(e);const rows=[...e.housingHistory].sort((a,b)=>(b.current?1:0)-(a.current?1:0)||String(b.start||'').localeCompare(String(a.start||'')));return `<div style="margin-top:20px"><h3 style="margin:0 0 10px">住居履歴</h3>${rows.length?rows.map(h=>`<div style="display:grid;grid-template-columns:minmax(220px,1fr) 150px 150px 120px;gap:12px;align-items:center;border:1px solid var(--line);border-radius:11px;padding:12px;margin-bottom:8px;background:#fff"><div><b>${h.type==='home'?'自宅：'+esc(h.address||'—'):esc(h.name||by(db.dorms,h.dormId)?.name||'—')}</b>${h.current?'<span class="badge green" style="margin-left:8px">現在</span>':''}</div><div><small class="muted">入居日</small><br>${displayDate(h.start)}</div><div><small class="muted">退去日</small><br>${h.current?'—':displayDate(h.end)}</div><div><small class="muted">本人負担額</small><br>${h.amount===''?'—':money(h.amount)}</div></div>`).join(''):'<div class="list-empty">住居履歴はありません。</div>'}</div>`}
 function render(e,edit=false){ensureHistory(e);const main=drawerBody.querySelector('.employee-detail-main')||drawerBody,d=e.dormId?by(db.dorms,e.dormId):null,home=isHome(e),count=d?residents(d):0,cap=Number(d?.capacity||0),current=e.housingHistory.find(x=>x.current);
  main.innerHTML=`<div id="housingAlert" style="display:none;margin:0 0 14px;padding:11px 13px;border-radius:10px;background:#eef6ff;border:1px solid #b9d5f5;color:#175a9c;font-weight:700"></div><div class="hml-bank-card"><div class="hml-bank-head"><div><h3 style="margin:0">現在の住居</h3><div class="muted">社員の現在の住居情報</div></div><button class="btn ${edit?'primary':''}" id="housingEditBtn">${edit?'保存':'編集'}</button></div><div class="hml-bank-grid">
  <div class="field"><label>住居情報 *</label><select id="housingDorm" ${edit?'':'disabled'}>${dormOpts(e.dormId||'',home)}</select></div>
  <div class="field"><label>入居日 *</label><input type="date" id="housingStart" value="${esc(current?.start||e.housingStartDate||'')}" ${edit?'':'readonly'}></div>
  <div class="field" style="grid-column:1/-1"><label>住所 *</label><input id="housingAddress" value="${esc(d?.address||e.address||'')}" ${edit&&home?'':'readonly'}></div>
  <div class="field"><label>本人負担額</label><input value="${d?money(employeeCost(d)):''}" readonly></div><div class="field"><label>駐車場</label><input value="${d?parking(d):''}" readonly></div><div class="field"><label>備品</label><input value="${d?esc(equipment(d)):''}" readonly></div><div class="field"><label>入居状況</label><input value="${d?(count+' / '+cap+'人'):''}" readonly></div>
 </div></div>${historyHtml(e)}`;
  drawerFoot.innerHTML='<button class="btn" onclick="closeDrawer()">閉じる</button>';document.getElementById('housingEditBtn').onclick=()=>edit?commit(e):render(e,true);const sel=document.getElementById('housingDorm');if(edit)sel.onchange=()=>{const val=sel.value,addr=document.getElementById('housingAddress');if(val==='__home__'){addr.value=e.housingType==='home'?e.address||'':'';addr.readOnly=false;addr.focus()}else{const nd=by(db.dorms,val);addr.value=nd?.address||'';addr.readOnly=true}preview(val)};
 }
 function preview(val){const d=val&&val!=='__home__'?by(db.dorms,val):null,inputs=[...drawerBody.querySelectorAll('.hml-bank-grid .field input')];if(inputs.length<6)return;if(d){inputs[2].value=money(employeeCost(d));inputs[3].value=parking(d);inputs[4].value=equipment(d);inputs[5].value=`${residents(d)} / ${Number(d.capacity||0)}人`}else{inputs[2].value='';inputs[3].value='';inputs[4].value='';inputs[5].value=''}}
 function commit(e){const sel=document.getElementById('housingDorm'),addr=document.getElementById('housingAddress'),start=document.getElementById('housingStart'),val=sel.value;if(!val)return err('住居情報を選択してください。',sel);if(!start.value)return err('入居日を入力してください。',start);if(val==='__home__'&&!addr.value.trim())return err('自宅の住所を入力してください。',addr);ensureHistory(e);const current=e.housingHistory.find(x=>x.current),newType=val==='__home__'?'home':'dorm',newDorm=newType==='dorm'?val:'',newAddr=newType==='home'?addr.value.trim():(by(db.dorms,val)?.address||''),changed=!current||current.type!==newType||current.dormId!==newDorm||current.address!==newAddr;if(changed&&current&&current.start&&start.value<current.start)return err('新しい入居日は現在の住居の入居日以降を指定してください。',start);
  if(changed){if(current){current.current=false;current.end=start.value}const d=newDorm?by(db.dorms,newDorm):null;e.housingHistory.push({id:'housing_'+Date.now(),type:newType,dormId:newDorm,name:d?.name||'',address:newAddr,amount:d?employeeCost(d):'',start:start.value,end:'',current:true})}else if(current){current.start=start.value}
  e.dormId=newDorm;e.housingType=newType;e.address=newAddr;e.housingStartDate=start.value;const now=new Date().toISOString();e.history=e.history||[];e.history.push({date:now,text:'住居情報を更新'});e.updatedAt=now;e.updatedText='住居情報を更新';save();render(e,false);if(typeof showSavedStatus==='function')showSavedStatus()}
 function err(msg,el){const a=document.getElementById('housingAlert');if(a){a.textContent=msg;a.style.display='block';a.style.background='#fff4f4';a.style.borderColor='#e6aaaa';a.style.color='#a22'}if(el)el.style.borderColor='#d94848'}
 const base=openEmployee;openEmployee=function(id,tab='dados'){base(id,tab);if(tab==='moradia'){const e=by(db.employees,id);setTimeout(()=>render(e,false),130)}};
 };wait();
})();